# Local persistence integration

Implementation worker: agent_muebc3bb_yqbj1wm. Ownership released to controller agent_mueapu6k_u5nkaij at 17:33 UTC on 2026-09-23 for integration and combined delivery.

The provider will pass `LocalAgentOptions.taskId` from the registered conversation ID and `checkpoint: { load(): unknown; save(value: unknown): Promise<void> }`. StructuredSessions supplies these closures against its existing local database settings, scoped by project and registered conversation, with live-runtime lifetime checks. The payload is opaque JSON: the agent owns schema validation, input, tool/result pairs, pending mutation identities, crash recovery, and lifecycle state. Save failures must stop mutation; restored pending work must never run automatically.

LocalAdapter.start restores the session without dispatching a turn or starting a model server. `runStatus()` exposes `session.state()`. Changing views does not resubmit input. The provider probes authenticated `/props` and `/v1/models` after readiness, using the smaller of configured context and reported runtime context. Missing probes remain explicitly unverified; template metadata is diagnostic only and never implies thinking mode. Agent owns exact apply-template/tokenize accounting.

Owned files: providers/local.ts, providers/adapter.ts, narrowly StructuredSessions.options, local-models/session-checkpoint.ts and focused tests. No agent.ts edits, live generation, smoke, build, push, or app.update by this worker. Local shipping waits for the controller integration.

Metadata-only inspection at 2026-09-23 16:30 UTC of the existing port 51435 server confirms `/props.default_generation_settings.n_ctx=32768` and `/v1/models.data[0].meta.n_ctx=32768`; `n_ctx_train=262144` must not be used as live capacity. Model alias is `local/ornith1.5-9b`, template length 7828, and template caps support tools, tool calls, parallel calls, preserve reasoning, system role, typed content, object arguments and string content; reasoning effort is unsupported. These do not establish whether reasoning is enabled. No generation or server mutation was performed.

Controller integration detail: `StructuredSessions.ensure()` only registers and `runStatus()` currently consults a live adapter. For persisted status before the first explicit input after app restart, root may need lifecycle plumbing outside this worker's options-only scope. `LocalAdapter.start()` restores when connected; `compactContext()` now awaits `compactNow()` so the agent can checkpoint before/after manual compaction asynchronously.

Exact measurement integration: provider now passes `measureTokens: true` only after authenticated `/props` returned a valid object, and passes it in `retarget()` for a restored session's first ready turn. Root must add `measureTokens?: boolean` to LocalAgentOptions and the retarget Pick, and measure the final serialized request (including tools/template) in its client. Missing or malformed `/props` leaves this false, with metadata diagnostics. Usage events are per-request `scope: 'message'` with unique IDs so multiple local rounds are summed rather than overwriting the turn total; occupancy remains from the last exact request and is unknown for incomplete usage.

Focused verification at 16:38 UTC: 28 tests passed across `src/main/providers/local.test.ts`, `src/main/providers/local-context.test.ts`, `src/main/local-models/session-checkpoint.test.ts`, and `src/main/structured-sessions-local-checkpoint.test.ts`. Covers persistent registered identity across SQLite reopening, stale lifetime refusal, project isolation, corrupt checkpoint refusal, completed/cancelled/blocked/pending restoration without dispatch, effective server ceiling and incomplete usage. Typecheck passed before measureTokens integration; root's retarget Pick must include the new hook before final typecheck. Shipping is explicitly held by root while agent/client integration changes.

Worker ship paths: this note; `src/main/providers/local.ts`; `src/main/providers/adapter.ts`; `src/main/providers/local.test.ts`; `src/main/providers/local-context.test.ts`; `src/main/structured-sessions.ts` (options wiring only); `src/main/structured-sessions-local-checkpoint.test.ts`; `src/main/local-models/session-checkpoint.ts`; `src/main/local-models/session-checkpoint.test.ts`. Root must coordinate any additional StructuredSessions edits before scoped shipping.

Final handoff at 17:33 UTC: root confirms measureTokens retarget integration and typecheck passed. Updated repetition report assertions to total hardLimit 72, task.segmentLimit 24, and the matching displayed total. All 28 focused tests above passed again against the integrated tree. All worker-owned paths are released to root. Per root's final instruction, root delivers the remaining dependent files in a combined git.ship after its live tests; this worker performs no separate ship/build and makes no claim that those files are already committed.
