# Smoke repairs, UI rows of the whole-app verification (2026-09-24)

Rows from `whole-app-verification.md` (11-21, excluding 16/18 owned by the parallel repair). Every run went through `node scripts/smoke-lock.mjs -- ...` against a build of this tree (`npm.cmd run build`, rebuilt after the CSS fix). Commands run from the repo root.

| Row | Before | After | Root cause | Files changed | Command |
| --- | --- | --- | --- | --- | --- |
| 11 | FAIL `Requested docking option was not available after drag start` | PASS | Stale smoke. 030907e replaced the per-pane `.dock-*` zone overlays with Chrome-style drag (the drop target comes from the pointer: tab strip → insertion index, pane body → nearest edge, `tab-drag.ts`). After that: the maximized-drag step ended with a bare `dragend`, which a parked (off-screen) window correctly treats as "dropped outside every window" and tears the tab off; Settings is now sectioned (theme on Appearance); a hidden sidebar stays mounted (`.left-shell.rail-only`, `aria-hidden`); the browser is a main-process guest view (`data-browser-web-contents-id`), not a `<webview>`; Ctrl+W no longer closes an empty workspace (64c7a02). | `scripts/smoke-ui.mjs` | `node artifacts/whole-app-verification/attached.mjs ui` |
| 12 | FAIL `0 !== 1` always-on-top | PASS | Stale smoke. Parked windows never become always-on-top (030907e). The smoke now records the `window:detach` request and asserts `{alwaysOnTop: true}` was asked for and that no window actually is. Then **app regression**: the active tab had no hover feedback (the active-tab fill rules outrank `.pane-tab:hover`); fixed in CSS with a cascade unit test. Then stale smoke: the theme swap is a view-transition crossfade (`viewTransitionName = 'conductor-theme'`), not a `theme-changing` class. | `scripts/smoke-navigation.mjs`, `src/renderer/src/styles.css`, `src/renderer/src/layout/active-tab-hover.test.ts` (new) | `node scripts/smoke-navigation.mjs` |
| 13 | FAIL `.sidebar-session-rename` never focused | PASS | Stale smoke. The sidebar row now starts with the tab-list toggle, so `> button:first-child` double-clicked the toggle; it now targets `.sidebar-session-open`. | `scripts/smoke-workspace-restore.mjs` | `node scripts/smoke-workspace-restore.mjs` |
| 14 | FAIL (blocked by the row 11 seed) | PASS | Seed fixed by row 11. Stale smoke: project files open in the workspace document (`.workspace-document .file-tab`), not as pane tabs; the seeded project is `Workspace smoke` under the harness's projects root, so verify now compares against the project `prepare` recorded (temp file per port) instead of hard-coding `Smoke Managed`. The pre-crash text check (`markerVisible`) now normalises Monaco's NBSPs and is asserted; before this it was logged but never checked. | `scripts/smoke-recovery.mjs` | `node artifacts/whole-app-verification/attached.mjs recovery` |
| 15 | FAIL effort slider expected `3`, got `2` | PASS | Stale smoke. The slider now carries exactly the model's ladder (`supportedEffortChoices` drops `auto`), so low/high/xhigh is 0..2. The smoke asserts `max=2`, xhigh at 2, ArrowLeft to high at 1. | `scripts/smoke-session-controls.mjs` | `node scripts/smoke-session-controls.mjs` |
| 17 | FAIL strict mode, two `Resume conversation` buttons | PASS | Two deliberate controls, not a duplicate: the runtime banner's resume action (`runtime-banner.ts`, 030907e) and the composer send button in its `resume` state. The smoke asserts both exist and clicks the banner's. | `scripts/smoke-steering.mjs` | `node scripts/smoke-steering.mjs` |
| 19 | FAIL expected `Account default`, got `Medium (account default)` | PASS (18 checks) | Stale smoke. 9fb9450 made the effort control name the documented default behind Account default (display only). The model label stays `Account default`, and the existing checks still require no saved effort and no `--effort` launch arg. | `scripts/smoke-capability-sweep.mjs` | `node scripts/smoke-capability-sweep.mjs` |
| 20 | FAIL `Auto Fixer must not switch the provider permission mode` | PASS (9 checks) | Stale smoke. A new native conversation opens on the owner's remembered mode, which is Auto in a fresh profile (`structured-sessions.ts`; AGENTS.md "Dispatched Claude and Codex coworkers run in Auto"). The smoke now asserts that the Fixer is on `auto` and that the `router.dispatch` coworker is on `auto` as well. | `scripts/smoke-project-task-dispatch.mjs` | `node scripts/smoke-project-task-dispatch.mjs` |
| 21 | FAIL `.pane-tab` never visible | PASS | Stale fixture. `LauncherPane` now calls `window.conductor.remote.*`, which the vite fixture's stub lacked, so every tab threw on mount. The stub now includes `remote`, and a render failure reports the page errors instead of timing out. | `scripts/smoke-tab-groups.mjs` | `node scripts/smoke-tab-groups.mjs` |

Unit and type checks after the app change: `npx tsc --noEmit` clean; `npx vitest run src/renderer/src/layout/active-tab-hover.test.ts` 1/1.
