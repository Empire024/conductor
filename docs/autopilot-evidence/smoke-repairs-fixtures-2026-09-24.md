# Smoke repairs: fixture rows (2026-09-24)

Five rows from `whole-app-verification.md`. All five were stale smokes (behaviour changed on purpose); no app source changed. Each command ran under `node scripts/smoke-lock.mjs -- ...` against the current `out/` build.

- **16a `smoke-structured-agents.mjs` (Codex)**: FAIL -> PASS. Cause: 98b0b6e removed the settings dialog's sandbox and approval selectors, so the composer mode is now the only place to choose permissions, and new Codex tabs open on Auto. Fix: switch Auto -> Edit through the `Conversation mode` menu, assert that the old selectors are gone, and assert `settings.permission === 'accept-edits'`. With Edit, the `Allow once` card still appears. Command: `node scripts/smoke-structured-agents.mjs`.
- **18 `smoke-agent-control.mjs`**: hang -> PASS (6/6), clean exit. Causes: (1) the `.agent-control-links` strip was replaced by per-tab MAIN/COWORKER markers with a relationship popover (`AgentControlLinks.tsx`). (2) The close override answered button index 1, which is now `Cancel` on the "Work is still running" quit prompt, so `app.close()` never settled. (3) The old watchdog-free script hung on that close. Fix: assert the markers, the popover, the sidebar chain label and the detached-window marker. Answer the close prompts by label. Bound the control fetches (30 s), `app.close` (20 s, then taskkill the whole tree) and the run (180 s watchdog). Print the error and the last provider input on failure. Command: `node scripts/smoke-agent-control.mjs`.
- **39 `smoke-change-history.mjs`**: FAIL -> PASS. Cause: same Auto default as 16a. Auto answers the fixture's in-workspace edit approval itself, so no `Allow once` card is shown. Fix: assert that the tab is on Auto, that it completes, that the recorded `Auto allowed ...` notice is present, and that no `Allow once` button exists. All change-history checks are unchanged. Command: `node scripts/smoke-change-history.mjs`.
- **23 `smoke-usage-warning.mjs`**: FAIL -> PASS. Cause: since 9139839, the Processes flame applies only to a process that is still working (`aggregateProjectProcessUsage`, unit-tested as "keeps idle spend but clears its flame warning" and "settled native phase suppresses..."). The smoke checked it after the turn completed. Fix: check the flame while the fixture turn is still running, by tightening the cap to 1,150 (1,100 tokens = 96%), then restore it and assert that the flame and pill clear. After completion, assert that the composer pill remains and the Processes flame stays clear. Command: `node scripts/smoke-usage-warning.mjs`.
- **26 `smoke-local-admission.mjs`** (via `artifacts/whole-app-verification/slot.mjs local-admission`): FAIL -> PASS. Causes: since 9139839, `LocalAdapter.start()` (connect) validates setup only and never calls `ready()`/`ensureServer`, so connect cannot raise the refusal. Since 9c1b5da, a first turn would stop an idle 9B that this Conductor started (a run record with a live pid, which is the case here: pid 56696, port 51437) and load the 35B, so the smoke must never send one. Fix: keep the host-level `startServer` refusal. In the app, assert that connecting the 35B tab stays idle, shows no `Starting` notice and no `Cannot start` text, and writes no 35B run record. The 9B record and health stay unchanged. The 9B was healthy before and after the run, and the 35B never loaded.

Files changed: `scripts/smoke-structured-agents.mjs`, `scripts/smoke-agent-control.mjs`, `scripts/smoke-change-history.mjs`, `scripts/smoke-usage-warning.mjs`, `scripts/smoke-local-admission.mjs`.
