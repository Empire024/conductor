# Future agent identity prompt

Status: deferred until the core agent coding workflow is reliable enough to build Conductor from inside Conductor.

> Give every persistent agent a distinct 3D holographic avatar. Each identity should have its own recognizable form, material, color language, and slow idle rotation. Keep rendering lightweight, pause animation when hidden, respect reduced-motion preferences, and never let the avatar compete with code, prompts, approvals, or runtime status. Design the system so new providers and user-created agents can receive deterministic variants without bespoke assets.

Acceptance work for that future slice:

- Prove GPU and memory cost in the tab performance meter.
- Provide static and reduced-motion fallbacks.
- Keep identity stable across tabs, windows, restarts, and provider changes.
- Do not begin until agent prompting, approvals, persistence, and recovery pass an in-app dogfood workflow.
