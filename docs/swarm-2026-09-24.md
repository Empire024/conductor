# Swarm 2026-09-24: fix every open to-do

Orchestrator: wizard tab `agent_mufqassq_jwwbo01` (Claude Opus 5.5). Owner rules for this swarm:
- cheaper Claude labour (Sonnet/Haiku; Opus for the hard ones);
- Codex **gpt-5.6-sol** may be used up (the owner resets it);
- local models used a lot, and cloud work may wait;
- Claude weekly cap 75%.

Capacity: four native coworkers plus one local model server (docs/machine-profile.md). Every worker owns an
exclusive file set; workers never edit feature-list.md (the orchestrator ticks items), and each ships its own
files with `git.ship`.

## Wave 1 (dispatched 18:3x)

| Worker | Agent | Model | Items |
| --- | --- | --- | --- |
| W1 restart & runtime continuity | agent_mufv9yop_1m4tetm | opus[1m] high | restart-leaves-idle-tabs-disconnected, wizard-restart-request, wizard-answers-quit-dialog, 19c298e4, app-update-no-dialog-in-auto, local-server-stop-control |
| W2 typing lag | agent_mufva08i_cpd1t7p | opus[1m] high | typing-lag-long-conversation |
| W3 project task list | agent_mufva4u7_qbxsjlf | codex gpt-5.6-sol high | 3bbf4893, 24e307fa (task part), 9afebc80, 5a68facc, fb7fbf10 |
| W4 UI polish | agent_mufva8lk_9yecftz | sonnet medium | fe4662b9, feature-file-type-colors, 4ab12812, 96d8efee, 53cd24fb |
| L1 flaky test | agent_mufvave2_8fp6neo | local/qwen3.6-35b-a3b (contract) | flaky-durable-jobs-timeout-test |

## Wave 2 (as slots free up; file owners in brackets)

- **W5 Opus:** e1610f01 Windows background service keeping cloud and local runtimes alive across restarts, plus the
  quit prompt "background tabs or close all". Starts after W1 [src/main/index.ts, a new service package].
- **W6 gpt-5.6-sol:** 9fb1c9c5 + f15c9164 (merge queued messages into one turn) + 2ad233c6 (unsend a queued message), and
  fb515071 (refused turn → continue on the next model). Starts after W1 [structured-sessions.ts, providers/claude.ts, composer].
- **W7 Sonnet:** web app: f0f8aaf7 (phone keyboard), 32920a68 (new-conversation defaults), 3775a5ec (notification
  choices). Starts after W3 [src/phone/**].
- **W8 gpt-5.6-sol:** control-concurrent-requests (src/main/agent-control-server.ts), 2c712217 Processes board (recent and active only) and 44a4ba26 (app-control activity rows)
  [Processes panel, agent-control activity log].
- **W9 Opus:** 764a7740 CLI drawer under Chat, and the chat part of 24e307fa (paging, copy transcript, search).
  Starts after W2 [conversation pane].
- **W10 gpt-5.6-sol:** ship-delivery-speed [src/main/delivery.ts]; logic-loops v1 [new loops module + orchestration.ts
  AUTO_FIXER_INSTRUCTIONS].
- **W11 Opus:** durable-jobs-as-local-option; 22fc5f94 rollback to a known-good version.
- **W12 Opus → modules:** 9b15feab Ideas MVP (last).
- **Queued on agent-control.ts after W13 → W12 (wiring + viewing-background-tasks):** W18 Opus main-brain-succession; then the orchestrator hands off to a fresh wizard main once W13–W17 are done.
- **Later, low cost (Sonnet/Haiku):** token-accounting-repeated-context (headline totals re-count cached context per turn).
- **Local (Qwen):** durable-jobs-verification overnight soak; verify-and-close reads for approval-auto-refusal-evidence,
  codex-auto-owner-escalation and the invoicing-* items (report only; the owner decides on the invoicing program).

## Log

- L1 (local Qwen, flaky test): stopped after 54 rounds. vitest cannot run in the local Linux sandbox against the Windows
  node_modules (Rollup has no Linux native binary), so a test command cannot be a local model's pass/fail check. Its edit
  was broken (fake timers freeze until(); `vi` not imported) and was reverted. The orchestrator fixed it with a bounded 20 s
  wait: 86350e3. Local tasks must now be read/write work that needs no npm (reports, tables, docs, text transforms).
- 9a49df9: the task-list ship that looked cancelled had already reached Conductor and was delivered.

## Verification wave (owner rule 2026-09-24: loop `verify`, .conductor/loops/verify.md)

Everything ticked today is re-checked against the owner's original words and images, adversarially, before the
swarm is called done. Fable plans and judges; Sonnet runs the scenarios; the local model runs real local-model
scenarios. Items that fail go back to `[ ]` with evidence. Groups:
- **V1 local models:** local-models-save-tokens, local-turns-survive-restart, durable jobs (as a local option,
  verification), Idea Incubator. Scenarios include "meaning of life", 1 MB and 20 MB files, a 50-file task, GPU
  busy, no server running, an overnight job, and a restart mid-turn.
- **V2 conversation and typing:** typing-lag-long-conversation (10k events, pasted 1 MB), queue merge and unsend,
  refusal fallback, chat paging/find/copy transcript, the CLI drawer, and the Viewing state.
- **V3 app shell and restarts:** runtime host (restart and update mid-turn, several tabs), wizard restart
  request, the quit dialog, idle tabs after restart, rollback restore points, app.update in Auto, and
  concurrent control requests.
- **V4 panels and phone:** task list (500 tasks, archive, collapse), Processes board, sidebar/tab groups/icons,
  the phone web app (keyboard, defaults, notifications, tasks, Ideas), Scheduled tasks, and Logic loops UI.
- Pending controller ship (after W13 commits src/main/logic-loops/logic-loops.test.ts): .conductor/loops/verify.md,
  batch-delivery.md v4, this file and feature-list.md. Add 'verify' to the pinned list in "validates every checked-in
  seed loop" (logic-loops.test.ts:56); the first attempt failed only on that pin.
- Delivered, pending verification (not ticked, per batch-delivery v4): logic-loops (v1 ab4869b, v2 55aa7bb),
  local-models-save-tokens (9f48ec4, 8a981be, index.ts wiring in 55aa7bb), local-turns-survive-restart (9c0dcf2),
  9b15feab Ideas MVP (4f9bf46 plus the desktop slice below). Deferred inside Ideas: docs/ideas.md next steps.
- W12 wiring and viewing-background-tasks: 48ae029 (and agent-control wiring in HEAD); invoicing-bounded-recovery,
  invoicing-runtime-budget, invoicing-evidence-telemetry and viewing-background-tasks are delivered, pending verification (V4).
- Verifiers switched from Fable to Opus (owner: Fable too expensive); verify.md v2, batch-delivery.md v5.
- W18 main-brain-succession: 8f2d83d, delivered, pending verification; used by the orchestrator's own handoff after the next update.
- W15 durable-jobs-verification: aaaf72c, acceptance matrix docs/durable-jobs-acceptance-2026-09-24.md; soak stopped at a single-slot local queue timeout (judged in V1). Pending verification.
- FX2 local-models-save-tokens fixes: 1839003 (S4b, S7, S13b, S25; S11 via usage.limits localSavings in index.ts). Needs V1 re-verification.
- FX5 24e307fa Copy transcript archive: d9ae618, needs re-verification. FX1 Ideas wiring + launcher: 338bbec, needs re-verification.
- FX6 V3 reopens: 9ce9e3a (S2), f675e46 (S20), 19306d3 (S12). FX3 rollovers: 68b115e (full 6 h soak not run; RV1 runs it). RV1 dispatched: agent_mugjv2k0_9sfxj23.
- 2026-09-25 08:30 (successor orchestrator agent_mugozcvg): bookkeeping shipped 1837ca8. Claude weekly 69%, so new implementation goes to Sonnet.
  Queued briefs (dispatch as slots free, max 4 native): FX7 smoke-instances-never-leak, FX8 auto-close-finished-coworkers,
  FX9 processes-board-stale-working + token-accounting-repeated-context. Still busy: FX4, V4 (+executor), RV1 (+executor).
  The V2 executor (agent_mug27h2i) is done; tabs.close on it did not close the tab (it still reports one stale background task).
- 09:10 FX4 delivered ed322d5 (typing while streaming: p95 2282 -> 73 ms); burst freeze in main + streaming p95 remain -> FX10 (Sonnet, agent_mugqm2p4, dispatched 09:10).
  FX7 (Sonnet, agent_mugpe4c8) dispatched 08:45 after the owner saw leaked smoke "Quit Conductor?" dialogs on the second screen; killed 11 orphaned test app/runtime-host trees.
- RV1 (1b921f9, docs/verification/2026-09-25-rv1.md): ticked e1610f01, 19c298e4, control-concurrent-requests, 9b15feab. Reopened local-models-save-tokens,
  durable-jobs-as-local-option, durable-jobs-verification -> FX11 brief queued. New: smoke-v3-s12-restart fails on HEAD; restart after an Ideas explore did not relaunch in 120 s (queue FX12).
  6 h durable soak: not run; run it at night under smoke-lock --timeout-min 380 once FX7 lands and no other smokes queue.
- Claude weekly 70% at 09:10: implementation on Sonnet from here; Opus only for judging.
- 11:50 local: FX10 delivered 0aa42af (batched store writes, chunked broadcast) + 8f09b87 (compositor-only tab animations; swept FX11's launcher CSS hunks);
  review found broadcast reordering across flushes -> fixed 63f94c5 (one ordered outbox, regression test). No clean perf numbers yet (llama-server load).
- Owner decisions 2026-09-25 ~11:45: rollback verified with a test-mode installer stub (FX12 brief queued); the 3 invoicing-* items dropped;
  Claude weekly cap 80%, implementation back on Opus ("Sonnet will produce lower quality"). FX8 redispatched on Opus (agent_mugs0ki7).
  batch-delivery gates now 78/80. V4 time-boxed to ~45 min.
- 12:30 FX11 47ddc8c, FX8 634b1fa delivered; local update 0.1.53-local.1790331094833 built (HEAD 634b1fa), install waits for V4.
  FX9 (agent_mugszd5x) and FX12 (agent_mugszefq) dispatched on Opus.
- Verifier retro (owner: "use the data, improve the loop"): docs/verification/2026-09-25-verifier-retro.md. 224 scenarios planned vs 10-25 briefed,
  ~36 % never got a result; 9-10 h lost to hung app.close() teardowns (V4 group B waited 7 h 35 min, judge active 7 min in 9.4 h); 70 copied smoke
  scripts; 11 of 13 reopens real, 1 false positive, 1 mixed. Applied verify.md v3 (one Opus tab, day lane <=12 scenarios / 2.5 h, overnight lane,
  control run behind every REOPEN, shared verify-kit) and docs/verification/verifier-brief.md. V4 group D hung 2 h again: killed, V4 told to report.
  New to-do verify-kit (FX13 brief queued).
- 12:45 V4 report 8f96019: ticked 2c712217; reopened 3bbf4893 (Archived count counts the loaded page), 4ab12812 (one shared local icon); most others UNVERIFIED (time-box/hang).
  Installing update 0.1.53-local.1790331094833 now (interrupts FX9 agent_mugszd5x and FX12 agent_mugszefq: steer them "continue" after restart).
- 13:10 installed 0.1.53-local.1790331094833 (wizard app.update.install force); FX9/FX12 resumed with a "continue" steer after the restart.
  FX9 aefcbd7 (board truth + token accounting), FX14 282af46/058fb13/42a7828 (archive count, local icons, S12 smoke, Ideas restart; tab auto-closed),
  FX12 95ce477 (installer stub; S16/S17/S18 pass through it; also stopped test app.update publishing into the owner's feed), FX15 44d1f47 (control activity).
  All await one independent verify-v3 round once FX13's verify-kit lands. New to-do runtime-host-handle-inheritance (FX12 finding).
  Owner's local-updates feed holds ~3.7 GB of old 0.1.19 installers outside restore points: ask before deleting.
- 14:35 FX13 91ea8a5 (verify-kit), MAC 0951dff/41256dc (remote-jobs, nodes.*, docs/mac-node.md; waiting on the owner's Mac key + username; the Mac fell asleep ~14:30).
  Owner: phone secure terminal + 6-digit lock -> items phone-secure-terminal, phone-pin-lock; PHONE worker agent_mugxuai4 (Opus). Deleted 0.1.19 installers (owner). Installing 0.1.53-local.1790339391232.
- 14:45 installed 0.1.53-local.1790339391232: this wizard turn survived the restart (runtime host) but lost its PreToolUse hook -> to-do runtime-host-hook-endpoint, FX16 (agent_mugy63ol).
  Mac key authorized (jurajdubovec@jurajs-mac-mini, Mac-mini, arm64, macOS 27.0): MAC steered to Phases 2-4, 8, live jobs, report.
  VR1 (first verify-v3 round, agent_mugy65vy): smoke-leak, auto-close, board-stale, token-accounting, 44a4ba26, rollback + app-update via the stub. Claude weekly 73%.
