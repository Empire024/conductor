# V4 verification: panels, phone, control additions (2026-09-24 swarm)

Verifier: agent_mug7atbc_uuh0ra8 (Opus, planned and judged). Executor: agent_mug7fxq4_64ypujv (Sonnet medium).
Plan: 76 scenarios in groups A–G, written from the owner's item text in feature-list.md (no item links images).
Time-boxed by the controller on 2026-09-25: groups A–C ran; the group D smoke hung for 2 h and the controller killed it;
E, F and G were not run. Everything ran in parked instances (`CONDUCTOR_TEST_USER_DATA` temp profiles and temp
projects) under `scripts/smoke-lock.mjs`, against out/ built from HEAD aaaf72c.

Verdicts: **VERIFIED** means every scenario that matters passed. **REOPEN** means a scenario failed with evidence.
**UNVERIFIED** means the item was not run, or its key scenario could not be driven. That is not a pass, and it goes back
to the next verify batch rather than to a fixer.

Evidence lives under `artifacts/verify-v4/<group>/` (screenshots plus result.json). The line-per-scenario log is
`.conductor-scratch/verify-v4/results.md`. Smokes: `scripts/smoke-verify-v4-{a,b,c,d}.mjs`.

## Summary

| Item | Scenarios | Verdict | Failing / missing scenario and evidence |
|---|---|---|---|
| 5a68facc priority/weight on hover | A9 | VERIFIED | Tooltip reads "Priority: High · Weight: Heavy" / unmarked "Priority: Normal · Weight: Medium" (A/A9-hover-*.png) |
| 3bbf4893 archive after 14 days | A4–A7 | REOPEN | A4: see R1 below. Boundary A5 passes (14 d 1 h archived, 13 d 23 h not). |
| 24e307fa (task part) done hidden, load fewer | A1–A3, A11 | UNVERIFIED | A1–A3 pass (677 ms, 40 rows, no done rows, first page 10.3 KB of an 85 KB file, load-more reaches all 180 open, 0 duplicates). A11 (the owner's real feature-list.md) did not show `.project-backlog` within 5 s; the one retry never ran. See U1. |
| 9afebc80 collapse/expand long tasks | A8 | UNVERIFIED | The smoke never located the long task (waitForFunction timeout via search). See U2. |
| 2c712217 Processes board | B1–B8 | VERIFIED | 240+ old sessions in 5 projects: board opens in 638 ms with 9 rows; all 5 projects named; "Show older" adds one 25-row page ("Show more 25 of 335"); cross-project read 0.4 ms; an old session turned running appears without "show older"; an idle project's roll-up reads idle. B3 caveat below. |
| fe4662b9 resizable sidebar | C1–C5 | UNVERIFIED | Keyboard resize, clamp 200–480 px, persisted across relaunch (280→280), double-click reset (312→232) and a 900 px window all pass (C/C1–C5 png). A **mouse drag** of `.sidebar-resizer` did not change the width in automation. That is the owner's primary gesture. See U3. |
| 4ab12812 per-model local icons | C6 | UNVERIFIED | Blocked: no local model configured in the parked profile. The source (ProviderIcon.tsx:24-35) has 4 distinct glyphs plus a Cpu fallback; the picker screenshot A/A4-archived-shown.png shows Ornith/Qwen/Dolphin all with the **same** chip icon in the launcher. See U4. |
| 96d8efee close group / "Close this tab only" | C7–C9 | UNVERIFIED | The menu has both "Close this tab only" and "Close tab group" (C/C8-context-menu.png), but "Close group is the default" is unproven. The tab count stayed 2→2 after "Close this tab only", and a flat count cannot tell survivors from a collapsed group chip. C7 was not run. |
| 53cd24fb drag coworker group beside main | C10 | UNVERIFIED | NOT RUN (time-box). |
| feature-file-type-colors | C11, C12 | UNVERIFIED | NOT RUN (time-box). |
| f0f8aaf7 phone keyboard | D1–D3, D13 | UNVERIFIED | NOT RUN: D smoke HUNG, killed by the controller. |
| 32920a68 phone new-conversation defaults | D4–D6 | UNVERIFIED | NOT RUN: HUNG, killed by the controller. |
| 3775a5ec phone notification choices | D7, D8 | UNVERIFIED | NOT RUN: HUNG, killed by the controller. |
| fb7fbf10 phone Tasks section | D9, D10 | UNVERIFIED | NOT RUN: HUNG, killed by the controller. |
| 9b15feab Ideas (phone screen) | D11 | UNVERIFIED | NOT RUN: HUNG, killed by the controller. |
| ea74d128 Scheduled tasks | E1–E7 | UNVERIFIED | NOT RUN (time-box). |
| logic-loops | E8–E12 | UNVERIFIED | NOT RUN (time-box). |
| invoicing-bounded-recovery | F1–F3 | UNVERIFIED | NOT RUN (time-box). |
| invoicing-runtime-budget | F4 | UNVERIFIED | NOT RUN (time-box). |
| invoicing-evidence-telemetry | F5 | UNVERIFIED | NOT RUN (time-box). |
| viewing-background-tasks | F6–F9, D12 | UNVERIFIED | NOT RUN (time-box; D12 hung with group D). |
| main-brain-succession | G1–G11 | UNVERIFIED | NOT RUN (time-box). |

## REOPEN: failing scenarios for the next batch

**R1 (3bbf4893): the Archived section header counts loaded rows, not archived tasks.** Setup: a 504-task
feature-list.md where 250 done tasks have activity 15–60 days old; turn on Done and Archived in Project tasks. The
header shows "323/504 done", but the Archived section heading reads **"Archived 0"** with 0 rows, because the first
40-row page is all open tasks. The count is `archived.length` over the visible page
(src/renderer/src/components/ProjectBacklogPane.tsx:423), while the true figure is already in `board.summary.archived`.
Same with Done: 19 done rows shown out of 70 recent. Evidence: artifacts/verify-v4/A/A4-archived-shown.png,
A4-done-shown.png, A/result.json. Test to write: with the 504-task fixture, the Archived heading shows 250 and the Done
heading shows 70 before any "load more"; each section can reach its own rows without first paging through all open
tasks.

## UNVERIFIED: what the next verify run must do

- **U1 (24e307fa):** open the repo's real feature-list.md (copied to a temp project) and wait up to 30 s for the Project
  tasks pane. Record the open time, rows in the DOM and first-page bytes. It failed at 5 s. It is unknown whether this
  is a slow first reconcile (257+ tasks, each first seen gets an activity row) or a locator/project-selection issue in
  the smoke. If it takes over 2 s, it is a REOPEN.
- **U2 (9afebc80):** scroll to a 2,000+ character task by its `data-task-id` (not through search). Check that it is
  collapsed to a few lines, one click expands it, a second click collapses it, and the expand click opens neither edit
  nor dispatch. Take screenshots of both states.
- **U3 (fe4662b9):** do a real pointer drag (`mouse.down` on the `.sidebar-resizer` box, `mouse.move` +300 px in
  steps, `mouse.up`) and check that the width changes. First confirm with `document.elementFromPoint` that the resizer is
  the element under the cursor. The handler (Sidebar.tsx:144-162) looks right, so this may be a harness issue, but
  the drag is what the owner asked for. If it fails with the resizer confirmed under the pointer, it is a REOPEN.
- **U4 (4ab12812):** configure three local models in a parked profile, with no server and no download. Compare the icon
  markup in the model picker, the tab strip and the new-tab launcher. A/A4-archived-shown.png shows the launcher
  giving Ornith 1.5 9B, Qwen 3.5 9B, Qwen 3.6 35B-A3B and Dolphin X1 8B the same chip glyph. If the launcher is one of
  the surfaces the owner meant, this is a likely REOPEN.
- **96d8efee:** inspect group membership directly, not a flat tab count. The controller's X closes the whole group;
  "Close this tab only" leaves both coworkers open.
- **Group D:** `scripts/smoke-verify-v4-d.mjs` hangs; it wrote no DONE and ran for 2 h. Before rerunning, give every
  wait a timeout and a 12-minute overall watchdog. Partial output was left in artifacts/verify-v4/D/result.json and
  **is not a verdict**. For the next run: pairing recorded FAIL with null evidence but later calls went through. D3/D13
  were marked FAIL, but they compared the composer (layout y 427–566) against the stub's height (516) and ignored its
  offsetTop (336). Inside the stubbed visual viewport (336–852) the composer is fully visible (D/D3-new-conversation-keyboard.png).
  Rerun with offsetTop 0, the normal iOS case. The new-conversation screen showed only the prompt plus a "Settings ·
  <project> · Codex · GPT-5.6-Sol" row (D4-new-conversation.png). The phone Tasks call passes
  includeDone:false/includeArchived:false (src/main/phone-project-tasks.ts:20). `window.conductor.ideas.create` does
  not exist under that name.
- **Groups E, F, G:** `scripts/smoke-verify-v4-{e,f,g}.mjs` were drafted but never run. They are not shipped, so the
  next verifier writes them from this plan (.conductor-scratch/verify-v4/plan.md, groups E–G).

## Notes (no verdict)

- **A6 (archive of hand-checked tasks):** a task checked `[x]` in the file that the app has never seen gets a
  synthesized activity row stamped at first open (src/main/project-backlog.ts reconcile). So it archives 14 days after
  Conductor first *sees* it, not 14 days after it was done. Old `[x]` items with no recorded history stay in the "done"
  section for two weeks. Done is hidden by default, so this does not bring back the clutter. If the owner wants
  immediate archiving of old items, the fix is to take the date from git blame on the checkbox line.
- **B3:** an `agent_sessions` row seeded with `status='waiting_input'` and no live runtime rendered as "Finished". That
  is likely correct reconciliation of a stale row, not a bug. The attention marker should still be checked once with a
  live fixture waiting on an approval.
- **B5:** a project drill-in (`listProcesses(projectId)`, src/main/database.ts:1408) returns full history (169 rows).
  It is fast at this scale (1.9 ms) but unbounded.
