# VR1 verification, 2026-09-25: coworker lifecycle, Processes board, control activity, rollback

Verify loop v3, one Opus tab. Build: a clean worktree at **d2141be** (HEAD; docs-only after 41256dc), `npx electron-vite build`,
a real `node_modules` copy (a junction is refused by `build-local-update.mjs`, correctly). Every run was parked, under
`smoke-lock`, one at a time. Wall time 14:39 to 15:15 (about 36 min of the 2.5 h day lane).
Results: `artifacts/verification/2026-09-25-vr1/results.md` (table, raw kit rows, logs, screenshots).
New smokes on `scripts/verify-kit.mjs`: `scripts/smoke-verify-vr1-{leak,coworkers,board,control,update}.mjs`.

Counts: 14 day scenarios planned (12 plus B1 split into B1 / B1-stuck / B1-declared), 14 run, 0 NOT RUN. 5 fixer smokes re-run, all PASS.
Harness re-runs, not product failures: F-fx12 run 1 (junctioned node_modules; S17 needs S16's feed); U1 r1/r2 (the stub project
failed `LocalUpdateBuilder.unsupported`); C1 r1 (same stub gap; the status-bar history polls every 30 s); board r1 (`agents.status` reads `viewing`).
Load: `loadCheck()` never read quiet (2-3 mid-turn tabs in the owner's app, CPU 8-41 %). No perf numbers were taken, so this does not affect any verdict.

## Items

| item | verdict | scenarios | evidence / notes |
|---|---|---|---|
| `smoke-instances-never-leak` (2de1884, 8bd158c) | **VERIFIED** | L1 PASS 2/2, L2a PASS 2/2, L2b PASS 2/2, L3 PASS, F-quit PASS | A hung smoke is killed at its `--timeout-min` (exit 124, 61 s), and its Electron tree is gone 1 s later. If the shell that ran smoke-lock is killed, or smoke-lock itself (`/F`, no `/T`), the app tree is gone in 2 s. No native dialog window (`#32770`) appeared on an uncaught main-process error or on a quit with a running turn. Both were logged to `main-errors.log`. |
| `auto-close-finished-coworkers` (634b1fa) | **VERIFIED** | A1 PASS, A2 PASS, F-autoclose PASS (8/8) | 6 coworkers of one controller, 3 delivered: the 3 delivered close and are kept in closed tabs. The 7 settled fixture CLIs drop to 0 while the 3 undelivered tabs and the controller stay open. A delivered tab with an unsent draft, with a running background task, or controlling a live coworker stays open; a plain delivered tab in the same run closes. The real CLI memory figure (the owner's 10 GB) is overnight N1. |
| `processes-board-stale-working` (aefcbd7) | **VERIFIED** (one minor follow-up) | B1 PASS, B1-stuck PASS, F-board PASS; B1-declared FAIL 2/2 | Settled with background tasks reads Viewing. A finished coworker whose persisted row was forced back to `working` (the owner's DB state) reads "Finished 19s ago". Only the genuinely streaming tab reads Working (the control). With the renderer clock moved 36 min forward, the row reads "Viewing · background task stuck 36m 22s". **Follow-up (fixer claim, not the owner's must-have):** "stuck past its declared timeout" never fires for a Claude background Bash. Its tool row turns `completed` when the "running in background" tool_result arrives (the real CLI and `scripts/fixtures/fake-claude.mjs` BASH WAIT both send it), so only the 30 min default applies. Test to turn this into: B1-declared in `smoke-verify-vr1-board.mjs`. |
| `token-accounting-repeated-context` (aefcbd7) | **VERIFIED** (Claude path); Codex bullet overnight | T1 PASS 2/2, F-board PASS | The owner's "61M tokens for one worker", sent in the real CLI's shape: 34 turns × 3 calls, 600k cache read each, with the message usage repeated per content block and a turn aggregate `result`. Headline "663k tok" (expected 663,000); beside it "153k out · 61.2m cache reads"; overview Processed 663k. The provider total of 61,863,000 appears only in the tooltip. The bullet "never add Codex cumulative totals" is covered by unit tests only; overnight N3 checks it on a real Codex. |
| `44a4ba26` control activity (44d1f47) | **REOPEN** (narrow) | C1 FAIL 3/3, C2 PASS 3/3, F-fx15 PASS (6/6) | These work: one chip row per turn (Opened / Prompted / Interrupted / a struck-through failed rename / read N); "Opened by" and "Interrupted by" on the coworker; the status bar shows "Built a local update by VR1 coworker" (19 s after the call; it polls every 30 s); and none of it reaches a model (C2: the next prompt and `agents.history` hold none of it). **Failing:** `tabs.close` leaves **no "Closed by <controller>" notice** on the closed coworker, and the controller's chip reads **"Closed a tab"** instead of its title. Cause: `ControlActivityRecorder.describeCall` resolves `tabId` after the call has answered (`named(undefined, args.tabId)`, `src/main/control-activity.ts`), and by then the tab is gone, so the notice has no target. Control: "Interrupted by" (same notify path, tab still open) in the same run, and "Renamed by" (the same `tabId` lookup) in F-fx15. Test to turn this into: C1 in `smoke-verify-vr1-control.mjs`. Resolve the target before the close runs (or from the call's result). |
| `22fc5f94` rollback (95ce477) | **VERIFIED** through the installer stub | F-fx12 S18+S16 PASS, F-fx12 S17 PASS, R1 PASS | Rollback from the main window takes 2 clicks (open Versions, "Roll back to this version") plus the confirm. The stub records the older build's installer (sha512 matches), and the relaunched app reports that version. Restore points record the commit, the claude/codex/grok CLI versions and the model catalogs of 4 providers. A rollback during a running turn keeps the turn. **Scope note for the controller:** a rollback restores Conductor and shows which CLI versions each build ran with. It does **not** reinstall an older Codex/Claude CLI, which is the owner's example ("if we update codex CLI and something stops working"). A real installer is NOT RUN (owner decision). |
| `app-update-no-dialog-in-auto` (95ce477) | **VERIFIED** | F-fx12 S18 PASS, U1 PASS | A Claude coworker in Auto ran 3 builds with no prompt (S18). A **Codex** coworker in Auto got `app.update` 200, `authorizedBy: 'auto'`, with 0 pending confirmations (U1). A Claude tab below Auto was asked and declined (control). AGENTS.md says the same. The local-model tab keeping the dialog is unit-tested only; it is overnight N4. |

## NOT RUN

| scenario | reason | unblocks it |
|---|---|---|
| Real installer run for a rollback | NOT RUN (owner): owner decision 2026-09-25, use the stub | owner |
| Real Claude CLI memory released by autoclose (the 10 GB figure) | overnight (real provider turns) | N1 |
| Codex cumulative thread total never added | overnight (real Codex) | N3 |
| Local-model tab still asks for app.update | overnight (needs the llama server; no second server by day) | N4 |

## Overnight queue

`.conductor-scratch/verify-overnight/vr1.md`: N1 autoclose with real Haiku coworkers and `git.ship` (CLI count and RSS);
N2 real Claude token headline against the CLI's own `result.usage`; N3 the same on real Codex; N4 local-model
`app.update` dialog and `app.update.authorize`; N5 leak soak (`node scripts/smoke-verify-vr1-leak.mjs --soak 10`);
N6 full `smoke-fx12-rollback.mjs` on the night's HEAD.
