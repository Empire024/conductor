Batch 2, Task B2: apply the confirmed capability repairs from the 2026-09-21 sweep. Read artifacts/swarm-2026-09-21/coworker-rules.md (binding) and the "Repairs (proposed, not applied in this batch)" section of docs/conductor-provider-parity.md, which names every repair with file and line as of today. The controller has just committed batch 1, so start from the working tree as it is now; other agents' uncommitted lines are still present in the files the rules list and must stay untouched. You never commit, push, checkout or reset.

Scope: R1, R2, R4, R7, R8, R9, R10, R11, R12, R14, R15, and R13 if it is small. Leave R3, R5 and R6 (fast tiers, richer catalog metadata, the GPT-5.5 entry) as written follow-ups in the document; do not build them.

Ownership: src/main/agent-manager.ts and agent-manager.test.ts; src/main/providers/codex.ts and codex.test.ts; src/main/providers/factory.ts; src/main/providers/local.ts and local.test.ts; src/shared/model-effort.ts and new model-effort.test.ts; src/shared/agent-model-selection.ts and its test; src/renderer/src/panes/composer-settings.ts and its test; src/renderer/src/panes/StructuredComposerControls.tsx; docs/conductor-provider-parity.md (mark each repair applied or deferred, with the evidence); docs/codex-compatibility.md and docs/claude-compatibility.md if a baseline changes; scripts/smoke-capability-sweep.mjs and its fixtures; artifacts/swarm-2026-09-21/capabilities/. Protected files opened to you for small additive hunks only, because the repairs need them: src/main/providers/claude.ts (R14 modelUsage lookup, R9 baseline constant) and claude.test.ts (append tests), src/shared/models.ts (R1: the AgentEffort union), src/shared/structured-agent.ts only if a type must widen. Nothing else; StructuredAgentPane.tsx stays closed.

Method: one repair at a time, each with its unit test, then npx tsc --noEmit. For R7, the fix must not send --effort at all when neither the owner's saved effort nor a catalog default applies, and the composer must show the runtime-reported effort once system/init arrives. For R4, prefer not passing --model before discovery so the CLI applies the owner's saved model; if a fallback must remain, it is opus[1m]. For R1, keep the legacy PTY guard on max unless you re-verify it, and let structured sessions use max and ultra. For R14 add the [1m] case to the existing context-accounting test. For R11 extend the compaction test so both notices survive. Then rebuild (npm.cmd run build; say so; nobody else is building now) and rerun scripts/smoke-capability-sweep.mjs in offline mode with your fixtures to show the repaired behaviours (context window on opus[1m], labels, effort), plus one live discovery-only run; at most four one-word live turns if a live check is unavoidable, and say how many you spent.

Report the file list, each repair's status with its evidence, the smoke result, and anything left undone.
